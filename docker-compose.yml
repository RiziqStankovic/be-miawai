services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: miawai
      POSTGRES_USER: miaw
      POSTGRES_PASSWORD: miaw
    ports:
      - "5432:5432"
    volumes:
      - miawai-postgres:/var/lib/postgresql/data

volumes:
  miawai-postgres:
